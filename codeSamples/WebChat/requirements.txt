# requirements.txt - Required Libs, 
#  as per setup instruction:  
#  NOTE see AIE/Courses/2503_ESA/...demos for new / later setup
# python -m virtualenv -p=/usr/local/bin/python3.12 .venv
# source .venv/bin/activate
# python3 -m pip install -r requirements.txt
# Blank lines and Comments (prefix by #) are allowed here for formatting

# On Viv Python 3.12 on MacOX 10.5.9 pip installing  notebook, openai, anthropic  cause it to error:
#  Building wheel for maturin (pyproject.toml): finished with status 'error'
#    from: error: can't find Rust compiler  (rust not supported on OS 10.9.5, rustup command fails
#    SO give up and use Ubuntu!

flask==3.0.2
flask-cors==4.0.0
flask-limiter==3.5.0
pytest==8.0.2
python-dotenv==1.0.1
anthropic==0.18.1
openai==1.12.0
mistralai==0.0.10
llama-api==0.1.0

seaborn
# Added for images:
Pillow

# For migration to WebChat.py"
flask
flask-cors
